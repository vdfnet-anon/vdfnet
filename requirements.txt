torch>=1.10.0
torchvision>=0.11.0
numpy>=1.21.0
opencv-python>=4.5.0
scikit-image>=0.18.0
scipy>=1.7.0
imageio>=2.9.0
Pillow>=8.0.0
tensorboard>=2.6.0
tqdm>=4.60.0
# timm is required by the IGEV backbone feature extractor (igev_baseline/) and by
# the Table I / Table II reproduction scripts (reproduce_table1.py / reproduce_table2.py).
timm>=0.9.0
# Optional: NVIDIA apex for mixed-precision (--amp). Install from source if needed:
#   https://github.com/NVIDIA/apex
# The deformable-conv CUDA extension (GwcNet/AANet backbones) is built separately:
#   cd nets/deform_conv && bash build.sh
